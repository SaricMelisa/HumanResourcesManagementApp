<!DOCTYPE html>
<html>
<head>
    <title>Candidate Profile</title>
    <link rel="stylesheet" href="/stylesheets/styleCandidateProfile.css">
</head>
<body>
    <header>
        <h1>Candidate Profile</h1>
    </header>
    <main>
        <form action="/candidate/profile" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="full_name">Full Name:</label>
                <input type="text" id="full_name" name="full_name" value="<%= profile?.full_name || '' %>" required>
            </div>
            <div class="form-group">
                <label for="phone_number">Phone Number:</label>
                <input type="text" id="phone_number" name="phone_number" value="<%= profile?.phone_number || '' %>">
            </div>
            <div class="form-group">
                <label for="address">Address:</label>
                <textarea id="address" name="address"><%= profile?.address || '' %></textarea>
            </div>
            <div class="form-group">
                <label for="skills">Skills:</label>
                <textarea id="skills" name="skills"><%= profile?.skills || '' %></textarea>
            </div>
            <div class="form-group">
                <label for="education">Education:</label>
                <textarea id="education" name="education"><%= profile?.education || '' %></textarea>
            </div>
            <div class="form-group">
                <label for="experience">Experience:</label>
                <textarea id="experience" name="experience"><%= profile?.experience || '' %></textarea>
            </div>
            <div class="form-group">
                <label for="cv_file">Upload CV:</label>
                <input type="file" id="cv_file" name="cv_file" accept=".pdf,.doc,.docx">
                <% if (profile?.cv_file_path) { %>
                    <p>Current CV: <a href="<%= profile.cv_file_path %>" target="_blank">Download</a></p>
                <% } %>
            </div>
            <button type="submit">Save Profile</button>
        </form>
    </main>
</body>
</html>
